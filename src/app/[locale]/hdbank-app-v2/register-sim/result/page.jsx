import { Suspense } from "react";
import ResultPage from "./ResultPage";

const HDBankRegisterSimResultPage = () => {
  return (
    <Suspense>
      <ResultPage />
    </Suspense>
  );
};

export default HDBankRegisterSimResultPage;
