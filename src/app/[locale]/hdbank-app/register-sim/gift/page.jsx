import { Suspense } from "react";
import GiftSimPage from "./GiftSimPage";

const HDBankRegisterSimGiftPage = () => {
  return (
    <Suspense>
      <GiftSimPage />
    </Suspense>
  );
};

export default HDBankRegisterSimGiftPage;
