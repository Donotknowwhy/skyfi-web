import { Suspense } from "react";
import SuccessPage from "./SuccessPage";

const HDBankActivateSuccessPage = () => {
  return (
    <Suspense>
      <SuccessPage />
    </Suspense>
  );
};

export default HDBankActivateSuccessPage;
