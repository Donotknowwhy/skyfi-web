import { Suspense } from "react";
import InfoPage from "./InfoPage";

const HDBankRegisterSimInfoPage = () => {
  return (
    <Suspense>
      <InfoPage />
    </Suspense>
  );
};

export default HDBankRegisterSimInfoPage;
